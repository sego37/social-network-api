# Socail Network API

```
In this application, we are able to create users,
see all users, find them by ID, udate them by ID, and
remove them by ID.

With these users, we can create thoughts, then see all
thoughts, find, update, and delete them by their ID.

With the useres, we can add and remove friends.

And we can aslo add reactions and remove reactions. 
```
